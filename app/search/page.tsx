type Props ={
    searchParams:{
        q: string;
    }
}

const SearchPage = ({searchParams: { q }}: Props) => {
    // Fetch the Search Results
    return (
        <div>SearchPage</div>
    )
}

export default SearchPage